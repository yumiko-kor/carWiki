
// component
import SubTitle from "../common/layout/SubTitle";

const CarHeader = ({ titleText, buttonText }) => {
    return (
        <>
            <SubTitle titleText="자동차 관리" buttonText="자동차 등록" />
        </>
    );
};

export default CarHeader;